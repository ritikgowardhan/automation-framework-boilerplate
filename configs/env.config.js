/**
 * env.config.js
 *
 * Description:
 * Environment configuration file used to manage different runtime environments
 * such as local, QA, staging, and production.
 *
 * Responsibilities:
 * - Store base URLs
 * - Store environment-specific variables
 * - Allow switching environments using .env or CLI parameters
 *
 * Used By:
 * Framework initialization and test execution.
 */